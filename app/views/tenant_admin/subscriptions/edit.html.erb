<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <div class="flex items-center gap-2">
      <%= link_to tenant_admin_subscription_path(@subscription), class: "text-gray-500 hover:text-gray-700" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
      <% end %>
      <h1 class="text-2xl md:text-3xl font-bold text-gray-900">Edit Subscription</h1>
    </div>
    <p class="mt-1 text-sm text-gray-500">Website: <%= @subscription.website.subdomain %></p>
  </div>

  <%= render 'form' %>
</div>

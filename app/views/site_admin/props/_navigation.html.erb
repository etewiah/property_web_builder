<!-- Tab Navigation -->
<div class="border-b border-gray-200">
  <nav class="flex -mb-px" role="tablist">
    <%= link_to edit_general_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'general' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      GENERAL
    <% end %>

    <%= link_to edit_text_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'text' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      TEXT
    <% end %>

    <%= link_to edit_sale_rental_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'sale_rental' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      SALE / RENTAL
    <% end %>

    <%= link_to edit_location_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'location' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      LOCATION
    <% end %>

    <%= link_to edit_features_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'features' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      FEATURES
    <% end %>

    <%= link_to edit_photos_site_admin_prop_path(@prop),
        class: "px-6 py-4 text-sm font-medium border-b-2 #{current_tab == 'photos' ? 'text-blue-600 border-blue-600' : 'text-gray-500 border-transparent hover:text-gray-700'}" do %>
      PHOTOS
    <% end %>
  </nav>
</div>
